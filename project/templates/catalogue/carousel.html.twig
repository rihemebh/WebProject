<!------ Include the above in your HEAD tag ---------->


<div class="container text-center ">
    <div id="recipeCarousel" class="carousel slide w-150" data-ride="carousel">
        <div class="carousel-inner w-150" role="listbox">
            <div class="carousel-item row no-gutters active">
                {% for livre in filtre %}
                    {% if loop.index <= 3 %}
                        <div class="col-4  float-left card-col">
                            <div class="card border-white " id="{{ livre.id }}" >
                                <i  class="fas fa-heart disabled love"><span
                                            class="js-like small"> {{ livre.Likes | length }}</span></i>
                                <a href="/livre/{{ livre.id }}"> <img src="{{ livre.path }}"
                                                                      class="book-img  card-img-top"
                                                                      alt="..." style="width:80%"></a>

                                <p class="card-title small " ><b>{{ livre.nomLivre }}</b>
                                    <a class="js-like " href="/livre/{{ livre.id }}/like">
                                        {% if app.user and livre.isLiked(app.user) %}
                                            <i style="font-size:60%;  " class="fas fa-heart text-danger hearticon"
                                               daéta-toggle="tooltip" data-placement="bottom"
                                               title="Unlove this book">
                                            </i>

                                        {% else %}
                                            <i style="font-size:60%" class="fas fa-heart hearticon"
                                               data-toggle="tooltip"
                                               data-placement="bottom" title="love this book">
                                            </i>

                                        {% endif %}
                                    </a>
                                </p>


                            </div>

                        </div>

                    {% endif %}
                {% endfor %}

            </div>
            <div class="carousel-item row no-gutters">
                {% for livre in filtre %}
                    {% if loop.index > 4 and loop.index <=7 %}
                        <div class="col-4  float-left card-col">
                            <div class="card border-white " id="{{ livre.id }}" >
                                <i  class="fas fa-heart disabled love"><span
                                            class="js-like small"> {{ livre.Likes | length }}</span></i>
                                <a href="/livre/{{ livre.id }}"> <img src="{{ livre.path }}"
                                                                      class="book-img  card-img-top"
                                                                       style="width:80%"></a>

                                <p class="card-title small " ><b>{{ livre.nomLivre }}</b>
                                    <a class="js-like " href="/livre/{{ livre.id }}/like">
                                        {% if app.user and livre.isLiked(app.user) %}
                                            <i style="font-size:60%;  " class="fas fa-heart text-danger hearticon"
                                               daéta-toggle="tooltip" data-placement="bottom"
                                               title="Unlove this book">
                                            </i>

                                        {% else %}
                                            <i style="font-size:60%" class="fas fa-heart hearticon"
                                               data-toggle="tooltip"
                                               data-placement="bottom" title="love this book">
                                            </i>

                                        {% endif %}
                                    </a>
                                </p>


                            </div>

                        </div>
                    {% endif %}
                {% endfor %}
            </div>

            <div class="carousel-item  row no-gutters">
                {% for livre in filtre %}
                    {% if loop.index > 8 and loop.index<=11 %}
                        <div class="col-4  float-left card-col">
                            <div class="card border-white " id="{{ livre.id }}" >
                                <i  class="fas fa-heart disabled love"><span
                                            class="js-like small"> {{ livre.Likes | length }}</span></i>
                                <a href="/livre/{{ livre.id }}"> <img src="{{ livre.path }}"
                                                                      class="book-img  card-img-top"
                                                                       style="width:80%"></a>

                                <p class="card-title small " ><b>{{ livre.nomLivre }}</b>
                                    <a class="js-like " href="/livre/{{ livre.id }}/like">
                                        {% if app.user and livre.isLiked(app.user) %}
                                            <i style="font-size:60%;  " class="fas fa-heart text-danger hearticon"
                                               daéta-toggle="tooltip" data-placement="bottom"
                                               title="Unlove this book">
                                            </i>

                                        {% else %}
                                            <i style="font-size:60%" class="fas fa-heart hearticon"
                                               data-toggle="tooltip"
                                               data-placement="bottom" title="love this book">
                                            </i>

                                        {% endif %}
                                    </a>
                                </p>


                            </div>

                        </div>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="carousel-item  row no-gutters">
                {% for livre in filtre %}
                    {% if loop.index > 12 and loop.index <=15 %}
                        <div class="col-4  float-left card-col">
                            <div class="card border-white " id="{{ livre.id }}" >
                                <i  class="fas fa-heart disabled love"><span
                                            class="js-like small"> {{ livre.Likes | length }}</span></i>
                                <a href="/livre/{{ livre.id }}"> <img src="{{ livre.path }}"
                                                                      class="book-img  card-img-top"
                                                                      style="width:80%"></a>

                                <p class="card-title small " ><b>{{ livre.nomLivre }}</b>
                                    <a class="js-like " href="/livre/{{ livre.id }}/like">
                                        {% if app.user and livre.isLiked(app.user) %}
                                            <i style="font-size:60%;  " class="fas fa-heart text-danger hearticon"
                                               daéta-toggle="tooltip" data-placement="bottom"
                                               title="Unlove this book">
                                            </i>

                                        {% else %}
                                            <i style="font-size:60%" class="fas fa-heart hearticon"
                                               data-toggle="tooltip"
                                               data-placement="bottom" title="love this book">
                                            </i>

                                        {% endif %}
                                    </a>
                                </p>


                            </div>

                        </div>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="carousel-item  row no-gutters">
                {% for livre in filtre %}
                    {% if loop.index >= 16%}
                        <div class="col-4  float-left card-col">
                            <div class="card border-white " id="{{ livre.id }}" >
                                <i  class="fas fa-heart disabled love"><span
                                            class="js-like small"> {{ livre.Likes | length }}</span></i>
                                <a href="/livre/{{ livre.id }}"> <img src="{{ livre.path }}"
                                                                      class="book-img  card-img-top"
                                                                      style="width:80%"></a>

                                <p class="card-title small " ><b>{{ livre.nomLivre }}</b>
                                    <a class="js-like " href="/livre/{{ livre.id }}/like">
                                        {% if app.user and livre.isLiked(app.user) %}
                                            <i style="font-size:60%;  " class="fas fa-heart text-danger hearticon"
                                               daéta-toggle="tooltip" data-placement="bottom"
                                               title="Unlove this book">
                                            </i>

                                        {% else %}
                                            <i style="font-size:60%" class="fas fa-heart hearticon"
                                               data-toggle="tooltip"
                                               data-placement="bottom" title="love this book">
                                            </i>

                                        {% endif %}
                                    </a>
                                </p>


                            </div>

                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        <
        </div>
        <a class="carousel-control-prev " href="#recipeCarousel" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon rounded-circle" style=" background-color: var(--nav_backcolor);"
                  aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#recipeCarousel" role="button" data-slide="next">
            <span class="carousel-control-next-icon shadow-lg rounded-circle "
                  style=" background-color: var(--nav_backcolor);opacity:1 !important;" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>


</div>
<br>